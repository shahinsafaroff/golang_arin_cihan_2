/*package main

import (
	"fmt"
	"os"
)

func main() {

	file, err := os.Open("file.txt")
	if err != nil { //detects an error and shows an error properly, so handles an error in a detailed way.
		fmt.Println(err)
	}else {
		fmt.Println(file.Name())
	}
}
*/